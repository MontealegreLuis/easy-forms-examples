{{ form_start(product) }}

{{ element_row(product.productId) }}
{{ element_row(product.name, {'label': 'Name', 'attr': {'id': 'name'}}) }}
{{ element_row(product.description, {'label': 'Description', 'attr': {'id': 'description'}}) }}
{# Override the element's default rendering block with the block option #}
{{ element_row(product.unitPrice, {'label': 'Unit price', 'attr': {'id': 'price'}, 'options': {'block': 'money'}}) }}

<button type="submit" class="btn btn-default">
    <span class="glyphicon glyphicon-edit"></span> Update information
</button>

{{ form_end() }}